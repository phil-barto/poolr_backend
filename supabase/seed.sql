-- seed.sql — local-only sample data. Loaded on `supabase db reset`.
-- Keep this reproducible and safe to run repeatedly. Do not put secrets here.

-- Example: seed data typically references auth users created via the local
-- Auth API or Studio. Add inserts here once you have test user IDs, e.g.:
--
-- insert into public.pools (id, owner_id, name)
-- values ('00000000-0000-0000-0000-000000000001', '<test-user-uuid>', 'Demo Pool')
-- on conflict (id) do nothing;
